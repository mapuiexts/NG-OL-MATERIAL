import { AddCoordinateDirective } from './add-coordinate.directive';

describe('AddCoordinateDirective', () => {
  it('should create an instance', () => {
    const directive = new AddCoordinateDirective();
    expect(directive).toBeTruthy();
  });
});
