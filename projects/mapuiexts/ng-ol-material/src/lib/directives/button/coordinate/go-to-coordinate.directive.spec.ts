import { GoToCoordinateDirective } from './go-to-coordinate.directive';

describe('GoToCoordinateDirective', () => {
  it('should create an instance', () => {
    const directive = new GoToCoordinateDirective();
    expect(directive).toBeTruthy();
  });
});
