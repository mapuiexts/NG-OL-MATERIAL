import { NolmDrawLineStringDirective } from './draw-linestring.directive';

describe('DrawLinestringDirective', () => {
  it('should create an instance', () => {
    const directive = new NolmDrawLineStringDirective();
    expect(directive).toBeTruthy();
  });
});
