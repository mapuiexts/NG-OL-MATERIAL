import { DrawPointDirective } from './draw-point.directive';

describe('DrawPointDirective', () => {
  it('should create an instance', () => {
    const directive = new DrawPointDirective();
    expect(directive).toBeTruthy();
  });
});
