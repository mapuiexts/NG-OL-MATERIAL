import { DrawBboxDirective } from './draw-bbox.directive';

describe('DrawBboxDirective', () => {
  it('should create an instance', () => {
    const directive = new DrawBboxDirective();
    expect(directive).toBeTruthy();
  });
});
