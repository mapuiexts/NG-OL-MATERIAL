import { WfsGetFeatureDirective } from './wfs-get-feature.directive';

describe('WfsGetFeatureDirective', () => {
  it('should create an instance', () => {
    const directive = new WfsGetFeatureDirective();
    expect(directive).toBeTruthy();
  });
});
