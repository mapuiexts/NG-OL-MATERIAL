import { WfsGetFeatureByPolygonDirective } from './wfs-get-feature-by-polygon.directive';

describe('WfsGetFeatureByPolygonDirective', () => {
  it('should create an instance', () => {
    const directive = new WfsGetFeatureByPolygonDirective();
    expect(directive).toBeTruthy();
  });
});
