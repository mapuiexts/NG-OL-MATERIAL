import { WfsGetFeatureByBboxDirective } from './wfs-get-feature-by-bbox.directive';

describe('WfsGetFeatureByBboxDirective', () => {
  it('should create an instance', () => {
    const directive = new WfsGetFeatureByBboxDirective();
    expect(directive).toBeTruthy();
  });
});
