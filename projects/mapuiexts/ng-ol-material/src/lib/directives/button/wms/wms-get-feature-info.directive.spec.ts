import { WmsGetFeatureInfoDirective } from './wms-get-feature-info.directive';

describe('WmsGetFeatureInfoDirective', () => {
  it('should create an instance', () => {
    const directive = new WmsGetFeatureInfoDirective();
    expect(directive).toBeTruthy();
  });
});
