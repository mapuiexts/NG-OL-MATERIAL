import { ClearMeasureDirective } from './clear-measure.directive';

describe('ClearMeasureDirective', () => {
  it('should create an instance', () => {
    const directive = new ClearMeasureDirective();
    expect(directive).toBeTruthy();
  });
});
