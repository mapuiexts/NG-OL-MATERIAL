import { GetDistanceDirective } from './get-distance.directive';

describe('GetDistanceDirective', () => {
  it('should create an instance', () => {
    const directive = new GetDistanceDirective();
    expect(directive).toBeTruthy();
  });
});
