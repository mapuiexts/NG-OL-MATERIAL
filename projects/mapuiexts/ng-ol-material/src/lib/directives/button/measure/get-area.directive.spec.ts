import { GetAreaDirective } from './get-area.directive';

describe('GetAreaDirective', () => {
  it('should create an instance', () => {
    const directive = new GetAreaDirective();
    expect(directive).toBeTruthy();
  });
});
