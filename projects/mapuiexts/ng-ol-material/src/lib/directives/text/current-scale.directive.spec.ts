import { CurrentScaleDirective } from './current-scale.directive';

describe('CurrentScaleDirective', () => {
  it('should create an instance', () => {
    const directive = new CurrentScaleDirective();
    expect(directive).toBeTruthy();
  });
});
